version: "2"
sql:
  - engine: "postgresql"
    queries: "api/db/queries"
    schema: "api/db/migrations"
    gen:
      go:
        package: "db"
        out: "api/internal/db"
        emit_json_tags: true
