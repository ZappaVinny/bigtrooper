version: "2"
sql:
  - engine: "postgresql"
    queries: "api/db/queries"
    schema: "api/db/migrations"
    gen:
      go:
        package: "db"
        out: "api/internal/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
